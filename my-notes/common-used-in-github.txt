shortest way to learn github

famous book: https://git-scm.com/book/en/v2

try verify in linkedin

write a book about how to use github

basic git workflow
1.modify files in my working tree.
2.selectively stage the file using git add
3.do a commit, take the staged files into Git directory

https://git-scm.com/book/en/v2/Getting-Started-What-is-Git%3F
Figure 6. Working tree, staging area, and Git directory
This figure perfectly shows the replationshiop with them

Three status:
committed:If a particular version of a file is in the Git directory
staged: If it has been modified and was added to the staging area
modified:And if it was changed since it was checked out but has not been staged(here means the file been added)
########################################################################################################################
basic:How to use git 
useful tricks in git

https://git-scm.com/book/en/v2/Getting-Started-Installing-Git
1.intall git

$ git config --global user.name "John Doe"
$ git config --global user.email johndoe@example.com
2.set your identity

3.get a git repository
3.1.You can take a local directory that is currently not under version control, and turn it into a Git repository
3.2.You can clone an existing Git repository from elsewhere.(from remote)
retult:you end up with a Git repository on your local machine, ready for work.

Initializing a Repository in an Existing Directory:
如果想用git管理本地文件需要在相应文件夹(根文件夹)，单击右键，选择“Open Git Bash here”
$ git init
now you don't have anything under git control
you just choose the exisiting directory
git don't know under this directory what file you want to control

tracked files and untracked files
https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository
Figure 8. The lifecycle of the status of your files
perfectly describe the status of all your files
4.
$ git status
check status of your files 
tells you which brand you're on
$ git status -s
short version of git status

tracking new files
$ git add README

staging modified files
Changes not staged for commit:a file that is tracked has been modified in the working directory but not yet staged

$ git add:tracking new files, to stage files

??:new files that aren’t tracked
A :new files that have been added to the staging area
M :modified files,already been added
two columns to the output
the left-hand column indicates the status of the staging area
the right-hand column indicates the status of the working tree


$ git add filename
$ git commit -m 'Initial project version'

Cloning an Existing Repository:
$ git clone https://github.com/libgit2/libgit2 mylibgit
mylibgit is the directory name you want to create
here you need to careful about where your command line is, cuz that's the location where clone to
url can be found here need picture to show...




git add (file)
在没有执行这项命令前，git没有管理任何在根目录下的文件
这项命令是把你需要git管理的文件加入

git status -s
简短的形式查看根目录所有文件的形式
??是不在管理的文件

git remote -v
查看upstram是什么

git remote add upstream https://github.com/selfteaching/the-craft-of-selfteaching.git
添加upstream

git branch
查看对应根目录下有多少个branch
我这里有
  master
* study

git branch study
创建一个名为study的branch

git checkout master
把工作目录切换到master这个branch。
注意：
在最开始的时候master和study这两个branch里面的内容是完全一致的。
但随着你不断的更改这两个branch的内容。
他们会变得不同，所以在你使用电脑工作时，记得switch到你想要运行的branch里。

git add .
把未管理文件/已更新的最新版 让git管理

git commit -am 'my study result'
commit之后会更新到git里面
-a 是add的意思这样git add就无需执行
-m 是message

git push
上传到远程仓库